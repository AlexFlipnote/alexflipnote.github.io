html
  head
    - var HEAD_TITLE = "AlexFlipnote - Branding"
    - var HEAD_DESC = "Personal branding assets for whatever usage"
    - var HEAD_PATH = "/branding"
    - var HEAD_ASSETS_JUMP = "../"
    include includes/_head

  body.github.dark-theme
    section.container
      h1.no-margin.text-center.sun-flower-text AlexFlipnote Branding
      #about.box-container
        h1.no-margin About
        p.
          I'm a Developer that spends time coding scripts and building websites.
          Releasing my branding materials if you desire to use it.
          Only requirement I have for using this is to give proper information
          about where the image comes from (alexflipnote.dev)

      #colours.box-container
        h1.no-margin Colour usage
        p.
          These colours are the correct HEX codes to the original avatar.
          The seasonal versions are not included in here.

        .flex-grid
          for g in brand_colours
            .colour(style="background-color: #{g.colour}; color: #{g.text}")
              span.name #{g.name} #[br] (#{g.pos})
              span.hex #{g.colour}

      #images.box-container
        h1.no-margin Current avatar
        p.
          The official avatar I use under the username #[b AlexFlipnote] online #[br]
          #[span.sun-flower-text Note:] This image is licensed under
          #[a(href="https://creativecommons.org/licenses/by-nc-sa/4.0/" target="_blank") CC BY-NC-SA 4.0]

        .flex-grid
          .col-xs-6.image-preview.center-flex
            img(src="assets/avatar_full.svg")
            span.image-text Full body
            .image-links
              a(download="avatar_full" href="assets/avatar_full.png" target="_blank") .png
              a(download="avatar_full" href="assets/avatar_full.svg" target="_blank") .svg

          .col-xs-6.image-preview.center-flex
            img(src="assets/avatar.png")
            span.image-text 1:1 avatar version (PNG only)
            .image-links
              a(download="avatar_full" href="assets/avatar.png" target="_blank") .png

    include includes/_footer
